package com.sdu.didi.uuid;

import android.os.Handler;
import android.os.Looper;
import android.os.Message;

class m
  extends Handler
{
  m(l paramL, Looper paramLooper)
  {
    super(paramLooper);
  }
  
  public void handleMessage(Message paramMessage)
  {
    switch (what)
    {
    }
    do
    {
      return;
      paramMessage = obj;
    } while ((paramMessage == null) || (!(paramMessage instanceof l.a)));
    l.a(a, (l.a)paramMessage);
  }
}
